/**
 *
 * SingingIcon
 *
 */
import { Icon } from '@material-ui/core';
import * as React from 'react';

export const SingingIcon = () => (
  <Icon
    className="fa fa-microphone-alt"
    fontSize="small"
    style={{
      width: '17px',
    }}
  />
);
